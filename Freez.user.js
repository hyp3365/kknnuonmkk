// ==UserScript==// @name         Freezehost 智能挂机助手 (监控倒计时版)// @namespace    http://tampermonkey.net/// @version      6.0// @description  监控倒计时状态，仅在卡顿或结束时动作，避免高频刷新检测// @author       Gemini// @match        *://*.freezehost.pro/earn*// @grant        none// ==/UserScript==(function() {    'use strict';    let lastCountdownValue = "";    let stagnantCount = 0;    function monitorAFK() {        // 1. 查找倒计时元素（通常是一个包含数字的 div 或 span）        const bodyText = document.body.innerText;        const countdownMatch = bodyText.match(/(\d{1,2}):(\d{2})/); // 匹配类似 18:09 的时间格式        if (countdownMatch) {            const currentCountdown = countdownMatch[0];                        // 检查倒计时是否卡住不走秒            if (currentCountdown === lastCountdownValue) {                stagnantCount++;            } else {                stagnantCount = 0; // 正常走秒，重置计数            }            lastCountdownValue = currentCountdown;            // 如果倒计时连续 60 秒没动（卡顿），刷新一次            if (stagnantCount > 6) {                 console.log("【监控】检测到倒计时卡顿，正在刷新...");                location.reload();            }            console.log("【监控】倒计时正常: " + currentCountdown);        } else {            // 2. 如果页面上没有倒计时，检查是否有“开始”按钮            const keywords = ["Start AFK Session", "开始挂机会话", "Start New Session", "开始新会话"];            const buttons = Array.from(document.querySelectorAll('button, a'));            const startBtn = buttons.find(b => keywords.some(k => b.innerText.includes(k)));            if (startBtn) {                console.log("【监控】倒计时消失，发现开始按钮，准备点击...");                // 延迟几秒再点，不要识别太快                setTimeout(() => {                    startBtn.click();                }, 3000 + Math.random() * 2000);             } else {                // 既没倒计时也没按钮，可能页面崩了，刷一下                console.log("【监控】状态异常，5秒后执行刷新重置...");                setTimeout(() => location.reload(), 5000);            }        }    }    // --- 关键点：降低扫描频率 ---    // 每 10 秒扫描一次，而不是 3 秒，减少被识别的风险    setInterval(monitorAFK, 10000);    // 每一分钟模拟一次滚动，保活    setInterval(() => {        window.scrollBy(0, 1);        setTimeout(() => window.scrollBy(0, -1), 500);    }, 60000);    console.log("【监控】挂机监控已启动，请手动开启首次会话以获得最佳稳定性。");})();
